Lambda:-
.cost-cutting
It is a serverless architecture which is automatically create , delete and manage the instances as it is connected with another services and automate the things which is already there in that services it is introduced so that the things will don't need manual task.

hands-on :- I have created a ec2 instance with this one volumes and one snapshot is also created with it so now after that i created a lambda and in this i change some permission by giving it the permission of delete snapshot and well as i also changes the execution time to 10 sec after this i have add a code into the lambda . Earlier what happened is when ec2 instances get deleted then that volumes is also deleted but the snapshot remain as  same so this lambda check is there any volume is there or not for that snapshot if no volumes for that snapshot then that snapshot automatically deleted.

The Lambda function fetches all EBS snapshots owned by the same account ('self') and also retrieves a list of active EC2 instances (running and stopped). For each snapshot, it checks if the associated volume (if exists) is not associated with any active instance. If it finds a stale snapshot, it deletes it, effectively optimizing storage costs.

this is code for this 
import boto3

def lambda_handler(event, context):
    ec2 = boto3.client('ec2')

    # Get all EBS snapshots
    response = ec2.describe_snapshots(OwnerIds=['self'])

    # Get all active EC2 instance IDs
    instances_response = ec2.describe_instances(Filters=[{'Name': 'instance-state-name', 'Values': ['running']}])
    active_instance_ids = set()

    for reservation in instances_response['Reservations']:
        for instance in reservation['Instances']:
            active_instance_ids.add(instance['InstanceId'])

    # Iterate through each snapshot and delete if it's not attached to any volume or the volume is not attached to a running instance
    for snapshot in response['Snapshots']:
        snapshot_id = snapshot['SnapshotId']
        volume_id = snapshot.get('VolumeId')

        if not volume_id:
            # Delete the snapshot if it's not attached to any volume
            ec2.delete_snapshot(SnapshotId=snapshot_id)
            print(f"Deleted EBS snapshot {snapshot_id} as it was not attached to any volume.")
        else:
            # Check if the volume still exists
            try:
                volume_response = ec2.describe_volumes(VolumeIds=[volume_id])
                if not volume_response['Volumes'][0]['Attachments']:
                    ec2.delete_snapshot(SnapshotId=snapshot_id)
                    print(f"Deleted EBS snapshot {snapshot_id} as it was taken from a volume not attached to any running instance.")
            except ec2.exceptions.ClientError as e:
                if e.response['Error']['Code'] == 'InvalidVolume.NotFound':
                    # The volume associated with the snapshot is not found (it might have been deleted)
                    ec2.delete_snapshot(SnapshotId=snapshot_id)
                    print(f"Deleted EBS snapshot {snapshot_id} as its associated volume was not found.")



step 1:- create a ec2 instance
step 2 :- create a snapshot of that ec2 instance
3:- create a lambda function and then code  python over the editor then ctrl + s and then click on deploy and test then create a test and save and click on test
4. go to permission then click on role name link then go to policy then create policy by selection ec2 instance then name that policy then save and go to roles , add permission , add policy then add that created policy then after come again test if it again showing something then again create that policy
5.go to ec2 delete that instance and also go to lambda click on test if it showing successfully execeuted then go to ec2 refresh if instance is not there then that snapshot will deleted.
note:- in this code it is coded that is any snapshot which is attach with a volume and that volume is not mapped with any ec2 then that snapshot will be deleted after that fixed time period.

to automate we use cloud watch to configure lambda with cloud watch we do this
1.open lambda function
2.add triger
3.eventbridge(cloudwatch event)
4.create a new rule
5.scheduled expression (eg, rate(1 day), rate(1 hour))
6.give rule name
7.save the trigger

Every Day
↓
EventBridge Trigger
↓
Lambda Runs Automatically
↓
Checks Snapshots
↓
Deletes Unused Snapshots

I created an EC2 instance, attached an EBS volume, and generated a snapshot. Then I developed a Python-based AWS Lambda function using Boto3 to identify orphaned snapshots. During implementation, I faced timeout and IAM permission issues, which I resolved by increasing the Lambda timeout to 10 seconds and creating an IAM policy with EC2 permissions such as DescribeSnapshots, DescribeVolumes, DescribeInstances, and DeleteSnapshot. Initially I tested the function manually using the Lambda Test feature, but for production use the Lambda can be connected to Amazon EventBridge (CloudWatch Events) to execute automatically on a scheduled basis and continuously clean up unused EBS snapshots, reducing storage costs and manual effort.



key-value pair :-A key pair acts as a security credential for an EC2 instance. It contains a public key and a private key. The public key is stored on the EC2 instance, and the private key is kept by the user. The private key is used to authenticate and securely connect to the EC2 instance via SSH.

instance type :- Instance Type defines the hardware configuration of an EC2 instance, such as CPU, memory (RAM), storage, and network performance.
aws provide variety of ec2 instance , user can use based on their requirement.
T-series (t2, t3, t4g) → General purpose, burstable workloads.
M-series → Balanced CPU and memory.
C-series → Compute-intensive applications.
R-series → Memory-intensive applications.

Network (VPC):- provides a private network for the EC2 instance.
 
Subnet :- determines the Availability Zone where the instance is launched.
 
Auto-assign Public IP :- allows the instance to be accessed from the internet.

Security Group (Firewall):- acts as a virtual firewall that controls traffic to and from the instance and helps secure it from unauthorized access.
